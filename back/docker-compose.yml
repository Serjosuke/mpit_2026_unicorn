version: "3.9"
services:
  api:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
    command: >
      sh -c "python -m alembic upgrade head && uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload"
